# Self-fleet implementer — edits orca's own Go code.
id: implementer
role: "Implementation engineer for the orca framework. Edits Go code and runs `go test`/`go build`."
runtime: claude-code-local
role_template: worker
runtime_opts:
  model: claude-sonnet-4-6
  permission_mode: bypassPermissions
workdir: .
tags: [code, implementer, go]
skills:
  - orca_comms
system_prompt_file: ./examples/prompts/self/implementer.md
budget:
  max_input_tokens: 15000000
  max_cost_usd: 40
  on_breach: soft_stop
